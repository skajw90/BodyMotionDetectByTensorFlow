# Uncomment the next line to define a global platform for your project
# platform :ios, '14.0'

target 'MotionDetectByTensorFlow' do
  pod 'TensorFlowLiteSwift', '~> 0.0.1-nightly', :subspecs => ['CoreML', 'Metal']
end
